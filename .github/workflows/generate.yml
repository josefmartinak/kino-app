name: Schedule Test

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "UTC:"; date -u
          echo "Local:"; date
